# backend/src/scrape

Read `/AGENTS.md` and `/backend/AGENTS.md` first.

Public HTML only. Check robots.txt. Identify as OutsetBot. Cap extra pages per domain at 3. If JSON-LD has no price, leave `price_cents` null and let completeness/gaps say so. Never write a slot table from a scrape. Detect FareHarbor, Peek, Checkfront, Rezdy, Xola, Booksy as `calendar_vendor` only.

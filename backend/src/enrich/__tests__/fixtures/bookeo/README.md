Skeggy's axe throwing, Easton PA (`bookeo.com/skeggys`), captured 15 September 2026 with `readBookeo`: the 302 to the account's shard, the start page, and per type the info popup, the `b_saveType` POST and the `b_saveRes` POST that reaches the people page (25 exchanges). The pages are stored with styles, comments, external script tags, SVG, link and meta tags, and inline scripts that carry no booking tokens removed, and whitespace collapsed; every element the parser reads is kept. Headers other than content-type, location and set-cookie were dropped.

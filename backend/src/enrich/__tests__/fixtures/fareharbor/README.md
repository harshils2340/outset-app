Jetski Miami Rentals, FareHarbor shortname `jetskimiamirentals`, captured 15 September 2026 with `readFareharbor` (37 exchanges: company, items, two calendar months, then availability, effective-sheets and price-sheet calls per item). Trimmed to the fields the reader uses: company and item records keep only the keys in `FhCompany`/`FhItem`, each calendar month keeps only the first bookable departure per item, availability documents keep only `customer_type_rates` (pk, party sizes, display name), price sheets keep only `price_previews.customer_types`. Headers other than content-type, location and set-cookie were dropped. Replayed by `replay.ts`.

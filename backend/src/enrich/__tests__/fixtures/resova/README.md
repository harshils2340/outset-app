Twisted Limits, McHenry IL (`twistedlimits.resova.us`), captured 15 September 2026 with `readResova`: the HTML shell, `misc/init` and the week's calendar (3 exchanges). Trimmed: calendar slots keep only item_id, type, duration, password, availability (private/min/max/available) and from-price, and each item's record appears on its first slot only. Headers other than content-type and set-cookie were dropped. Dates in request URLs are normalised on replay.

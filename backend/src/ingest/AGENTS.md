# backend/src/ingest

Read `/AGENTS.md` and `/backend/AGENTS.md` first.

`tampaUnclaimed.ts` is the hand-copied Tampa catalog. Those facts were pulled from each business's own site. `nationalTargets.ts` is official homepages for other US and Canada metros. Facts for those rows come from scrape, not invented prices. Demo listings from the guest app do not belong here.

# backend/src/db

Read `/AGENTS.md` and `/backend/AGENTS.md` first.

SQLite file lives at `backend/data/outset.db`. Schema changes go in `schema.sql`. Do not store invented inventory tables. Slots only exist after an operator claims and publishes them.

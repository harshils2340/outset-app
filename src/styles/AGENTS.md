# src/styles

Read `/AGENTS.md` first.

All visual tokens and component CSS live in `app.css`. Prefer existing classes (`.card`, `.chip`, `.cta`, `.sheet`) over new one-off styles. Dark theme is driven by `prefers-color-scheme` and `data-theme`.

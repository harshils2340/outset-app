# src/components/art

Read `/AGENTS.md` first.

`Art.tsx` renders scene SVGs from `src/data/art.ts`. Gradient ids must stay unique per listing id so cards do not leak fills. Add new scenes in `art.ts`, not inline in views.

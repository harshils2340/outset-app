# Nightly security sweep, 2026-09-21

Baseline is the last sweep, `docs/security/sweeps/2026-09-20.md`. That report cited commits
`f337a6f5a`, `e11894038` and `183a54975`, but none of those hashes exist in this checkout's
history any more: the repository's entire git history now starts at a single root commit,
`bf2392d78` (`Sun Sep 20 06:15:29 2026 +0000`), and runs 55 commits to the current head,
`2331c11de`, all dated 2026-09-20 or later. The tree itself carries the content every prior
report described as fixed: `backend/src/sync/pages.ts`/`listingPages.ts` still escape `<` to
`<` before embedding JSON-LD (the `e11894038` fix), and the concierge's `plan.ts` args
are still typed `(string | number)[]` (the `183a54975` fix). So this reads as a history
squash/reset that kept every file's content, not a rewrite that dropped work; flagging it for
Harshil rather than treating it as a finding, since a rewritten history is itself worth knowing
about and this sandbox cannot tell why it happened. With no old commit hash to diff against,
"since the last sweep" below means every commit dated 2026-09-20 or later (all 55 of them):
the rename of the product from Outset to GoDo across copy and emails, a second concierge
booking-system reader (Resova), an Agent Mode text-thread UI, a `/nearby` Places lookup and a
ForeUp golf reader, several bug-bash fixes, and eight bug-bash logs.

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a
fresh `npm ci` in both trees: all four runs came back "found 0 vulnerabilities." Nothing to
patch or bump.

**2. Secret scan.** Scanned every commit dated 2026-09-20 or later via `git log -p` (full
diff, excluding only the multi-hundred-thousand-line bulk discovery/OSM/batch data dumps in
`backend/data/discovered`, `backend/data/osm` and `backend/data/batches`, which are non-secret
catalog seed data and were still covered by the working-tree scan below), plus the full
working tree including `public/`, `docs/`, `backend/data/`, fixtures and `.github/`, for
`postgres(ql)://` URLs with a password, `github_pat_`, `ghp_`, `sk_live_`, `sk_test_`,
`rk_live_`, `whsec_`, `sk-ant-`, `sk-proj-`, AWS key IDs, Google API key shapes, SendGrid/
Mailgun/Resend key shapes, and generic `KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to long
random strings.

Same known, unchanged hit as every prior sweep: the `AKIAQHHKRFYJV24PICFC` presigned S3 key ID
in `public/o/o-culinarybackstreets-com.json` and `backend/data/photos/shard-1-of-10.json`
(culinarybackstreets.com's own vendor's key, long expired, not Outset's). One new
Google-API-key-shaped string, `AIzaSyCoSDhaFJ0ABL9pv_rTx0iH79EBeXLxPKk`, in a new doc comment
in `backend/src/concierge/readers/foreup.ts`: read the surrounding comment, and it names this
exact value as ForeUp's own public front-end key, shipped to every browser that loads any
course's booking widget (confirmed present verbatim on three unrelated golf courses' own
pages) and not scoped to any one account — the same shape of non-issue as the AKIA hit.
Everything else the patterns caught (`sk_test_recorder`, `whsec_e2e`, `whsec_recorder`,
`whsec_test`, a `RECAPTCHA_SITE_KEY`, two `CLAIM_SECRET`/`ADMIN_KEY` test fixtures) is a test
constant or a deliberately-public site key, same as every prior sweep. No `.env` file exists
in the repo; both `.gitignore`s still exclude it. No postgres URL with a password anywhere.
Nothing live-looking, nothing to rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build` (succeeds, same pre-existing
chunk-size warning as every prior sweep). `index.html`'s CSP is unchanged from the policy the
2026-09-16 rendering review documented and tested: `script-src 'self' https://js.stripe.com`,
`style-src` the Google Fonts stylesheet, `font-src` Google Fonts files, `img-src`/`media-src`
`'self' http: https:` (deliberately wide, documented in `docs/security/2026-09-16-rendering.md`
as covering hotlinked operator photos, including the still-live wsrv.nl proxy that most photos
route through), `connect-src` the API, the Photon geocoder and Stripe, `frame-src` YouTube/
Vimeo/Stripe, `object-src 'none'`, `form-action` Stripe checkout. The `Referrer-Policy` meta
tag (`strict-origin-when-cross-origin`) is also unchanged.

One real finding, not a vulnerability but host drift the checklist exists to catch: this
window's new `WebListing.tsx` (the unclaimed-listing web page) added a `TikTokScript`
component that injects `<script src="https://www.tiktok.com/embed.js">` to render a business's
TikTok creator embed, and a `<blockquote class="tiktok-embed">` alongside it. Neither
`www.tiktok.com` nor any TikTok CDN host is in the CSP's `script-src` or `frame-src`. Under the
current policy the browser blocks the script outright, so the embed silently never upgrades
and the page falls back to the plain "@handle on TikTok" link already rendered next to it — not
a security hole, since the CSP is doing exactly its job, but the feature is dead in production
today. Left as a report item rather than a same-night CSP change: whether to trust
`tiktok.com` as a script and frame source is a product call, not a mechanical fix.

`src/lib/here.ts` and the `crawl:here`/new `truth.mts`/`sniff.ts` backend scripts (read, not
run, per this sweep's standing rule against crawling operator sites from here) call no new
external host from the browser; `sniff.ts` and `truth.mts` are local-machine tools that talk to
individual operators' own booking pages when a human runs them, not something this build ships.

**4. Hacked-page screen.** Copied `isCompromisedText`/`isCompromisedPhrase` and their helpers
verbatim from `backend/src/sync/contacts.ts` (unchanged since the baseline) into a standalone
script under `/tmp`, run over every string field, recursively, in `public/catalog.json` and
every file in `public/o/*.json`.

Flagged tonight: **12** in `catalog.json`, **20** ids in `public/o/*.json` (34 raw field hits,
several ids trip on both a cover and a duplicate gallery photo) — the same counts as every
recent sweep. Every one of the 20 ids matches the last report's list exactly, including
`o-windsorgymnastics-org` (its cover is a `blogger.googleusercontent.com` redirect URL ending
in `.../pttogel-togel-online.webp`, unchanged). The 6 `isKeywordStuffed` hits
(`o-easttnscouts-org`, `o-islandchillyachtcharters-com`, `o-locationdesquatrelacs-com`,
`o-nortonslc-com`, `o-rfcity-org`, `o-scene75-com`) are the same real-rate-card/scraping-
artifact false positives every prior sweep named, not gambling spam. No id is new since the
baseline. No spam phrase got past the existing patterns, so no new phrase was needed tonight.

**5. Live checks, could not run, fifth night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` both got a `403` on the CONNECT tunnel. The proxy's
own status endpoint confirms `recentRelayFailures`: `connect_rejected`, "gateway answered 403
to CONNECT (policy denial or upstream failure)" for `onoutset.com:443`. Sandbox egress policy,
not a live-site problem. CSP/referrer meta tags, API security headers, the `/health` payload
and the CORS answer against `https://evil.example` were not verified tonight.

**6. `.github/workflows`.** Only `.github/workflows/e2e.yml` exists, and no commit dated
2026-09-20 or later besides the initial squash touched `.github/` at all. All three `uses:`
lines (`actions/checkout@11d5960a...`, `actions/setup-node@49933ea5...`,
`actions/upload-artifact@ea165f8d...`) pinned to a full commit SHA with a version comment.
Top-level `permissions: contents: read`, no per-job widening, single job never pushes.
`STRIPE_SECRET_KEY`, `RESEND_API_KEY` and `GITHUB_TOKEN` are all explicitly set to empty
strings in the job env; the only real secret it uses is `E2E_DATABASE_URL`, a scratch Neon
branch. Nothing unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. The only commit besides the initial squash to touch a package
manifest, `a5ec2cc2b`, added five new npm scripts to `backend/package.json`
(`cover-screen`, `avail:capture`, `avail:report`, `avail:judge`, `avail:rebaseline`) that all
run existing project code through the already-present `tsx`; it changed no `dependencies` or
`devDependencies` block and neither `package-lock.json` file changed at all in that commit.

## Fixed

Nothing. Every check came back clean or, for the two items above, not clear-cut enough for a
same-night mechanical fix (a CSP host addition is a product decision about trusting a new
third party; the pre-existing history rewrite is informational, not a bug to patch). No `npm
audit` bump, no stray secret, no new spam phrase, no workflow to re-pin.

Baseline verified anyway, to confirm tonight's read of a clean tree: root `npx tsc -b` clean;
backend `npx tsc -p tsconfig.json --allowImportingTsExtensions` clean; `npx vite build`
succeeds; backend `npm test` 533/533. Root `npm test` is 612/613 — see "Needs Harshil" below;
not fixed here since it is a product-logic bug (a chat answer's hours phrasing), not a security
finding, and outside tonight's fix scope.

## Needs Harshil

- **The repository's git history was squashed or reset on 2026-09-20**: every commit now
  descends from a single new root commit dated `2026-09-20T06:15:29Z`, and the exact hashes
  every prior sweep report cited (`f337a6f5a`, `e11894038`, `183a54975`, and further back)
  no longer exist anywhere in this checkout. The file content those commits produced is still
  present and was spot-checked tonight (the JSON-LD escaping fix and the concierge args typing
  fix are both still in the tree exactly as described), so this reads as an intentional
  history cleanup rather than lost work, but worth a plain confirmation that it was you or
  someone with legitimate write access, since a force-push that erases commit history is
  also what an attacker with a stolen token would do to cover tracks.
- **`WebListing.tsx`'s new TikTok creator embed is silently non-functional in production**:
  it loads `https://www.tiktok.com/embed.js` and expects to render an iframe, but the CSP in
  `index.html` allows neither `tiktok.com` in `script-src` nor in `frame-src`, so the browser
  blocks it and the page shows only the plain "@handle on TikTok" fallback link instead of the
  embed. If the embed is meant to work, `script-src` and `frame-src` need
  `https://www.tiktok.com` (and check what CDN host the resulting iframe actually loads from,
  since TikTok typically serves the embed iframe from a separate domain than `embed.js`
  itself). If the plain link is an acceptable permanent fallback, no CSP change is needed and
  this can be closed as-is.
- **Root `npm test` has one failing test**, unchanged in scope from a security sweep's
  concern but worth a look: `src/lib/__tests__/ottoScope.test.ts:231`,
  "a holiday is not answered with today's hours", asserts
  `assert.match(ask("are you open right now?"), /today at/)` and got `'No, closed now. Opens
  tomorrow at 9 AM.'` instead. This reads as a wall-clock-dependent test — it assumes the
  fixture shop is open at whatever real time the suite happens to run, which will not always
  be true — rather than a real regression in the hours logic; backend's own hours tests all
  pass. Not fixed tonight since it is application behavior, not a security check, and outside
  this sweep's remit.
- **The 14 published listings still carrying a gambling-spam banner as their cover or gallery
  photo**: `o-carrefouratlantic-com`, `o-dancestudioofmainesouth-com`,
  `o-dogmasterdistillery-com`, `o-firemensmuseum-com`, `o-littlewhiteschoolhouse-com`,
  `o-mainstreetarmory-com`, `o-markethousemuseum-com`, `o-rockysfitnesscenter-com`,
  `o-rosshillpark-com`, `o-salidagolfclub-com`, `o-suskyriver-com`,
  `o-thejoyofdancingsanjose-com`, `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`. Same
  fix as every prior sweep has asked for: re-run the hacked-page screen against the
  already-synced `facts`/`offerings` rows, quarantine the cover/photo rows that trip it, and
  re-sync. This sandbox cannot do it without production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-locationdesquatrelacs-com` (a legitimate repeated boat rate card), `o-scene75-com` (a
  repeated-word render/scraping artifact), `o-rfcity-org` and `o-nortonslc-com` (similar). Not
  urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`)
  could not run again, fifth sweep in a row. This sandbox's egress proxy still blocks both
  hosts by policy. Needs a re-run from somewhere with real internet access, or a manual check.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch
  protection, 2FA, Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password
  rotation, GitHub PAT scope, `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim
  links) is still open; this sweep didn't re-check items that need Render/GitHub dashboard
  access it doesn't have.

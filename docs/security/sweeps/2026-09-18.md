# Nightly security sweep, 2026-09-18

Baseline is the last sweep, `docs/security/sweeps/2026-09-17.md` (commit
`db0a577c1`). 33 commits landed since then, all bug-bash fixes and their
logs (`db0a577c1..HEAD`); nothing touched `package.json`, `backend/package.json`
or `.github/`.

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without
`--omit=dev`: all four runs came back "found 0 vulnerabilities." Nothing to
patch or bump.

**2. Secret scan.** Scanned all 33 commits since the baseline via `git log
-p`, plus the full working tree including `public/`, `docs/`,
`backend/data/`, `backend/src/enrich/__tests__/fixtures` and `.github/`, for
`postgres(ql)://` URLs with a password, `github_pat_`, `ghp_`, `sk_live_`,
`sk_test_`, `rk_live_`, `whsec_`, `sk-ant-`, `sk-proj-`, AWS keys, SendGrid
and Google API key shapes, Slack tokens, private-key headers, and generic
`KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to long random strings.

Two hits, both already known and unchanged from last night: the
`AKIAQHHKRFYJV24PICFC` presigned S3 access key ID in
`public/o/o-culinarybackstreets-com.json` and
`backend/data/photos/shard-1-of-10.json`. It is culinarybackstreets.com's own
vendor's key from crawling their photo CDN on 2026-09-14, its matching
signature was scoped to a 300-second window that expired months ago, and it
is not Outset's credential. One placeholder in a bug-bash log
(`STRIPE_TEST_SECRET_KEY=sk_test_...`, a literal ellipsis, not a real key)
also matched the pattern and was checked by hand. No `.env` file exists in
the repo, and nothing new or live-looking turned up.

**3. Third-party host drift.** Ran a fresh `npx vite build` and compared
every host in `dist/`, `index.html` and `src/` against the CSP in
`index.html` (`'self'`, `js.stripe.com`/`api.stripe.com`/`checkout.stripe.com`/
`hooks.stripe.com`, `fonts.googleapis.com`/`fonts.gstatic.com`, `wsrv.nl`,
`outset-api.onrender.com`, `photon.komoot.io`, YouTube/YouTube-nocookie/
Vimeo). All operator-site hostnames seen in the bundle
(`axethrowingtampa.com`, `bigredballoon.com`, etc.) are plain "visit their
site" / contact-data strings, not script, connect or frame destinations, so
they need no CSP entry. No new script, stylesheet, connect or frame host
appeared since the last sweep — `package.json`, `index.html`'s CSP meta tag,
and `vite.config.ts`'s `cspApiOrigin` plugin are all unchanged since
`db0a577c1`.

One pre-existing gap, not new tonight: `src/components/web/WebListing.tsx`
mounts a `<script src="https://www.tiktok.com/embed.js">` for an operator's
TikTok embed (added 2026-09-16, commit `7fde66e6d`, before the last sweep's
baseline), but `script-src` in the CSP meta tag only allows `'self'` and
`https://js.stripe.com` — the previous review's rendering pass and last
night's sweep both missed it. Checked whether it is live today: no listing
in `public/catalog.json` or `public/o/*.json` currently has a `tiktok`
field set, so no guest hits this path yet, but the moment a crawl adds one,
the browser's own CSP enforcement blocks the embed. See Needs Harshil; not
fixed tonight since it isn't "new since last sweep" and a CSP change is
outside tonight's fix list.

**4. Hacked-page screen.** Ran `isCompromisedText`/`isCompromisedPhrase`
from `backend/src/sync/contacts.ts`, unchanged since last night, over all
59,163 operators in `public/catalog.json` and all 59,162 files in
`public/o/*.json` via a script under `/tmp`. Same counts as last night: 13
flagged in `catalog.json` (spam-banner cover photos), 106 flagged in
`public/o/*.json`. Diffed listing ids against the 106 named in the
2026-09-17 report: **identical set, no new ids, none resolved** — no
database cleanup has happened between sweeps, as expected since that needs
production access this sandbox doesn't have. No spam pattern got past
`SPAM_LINE`/`HACKED_SCRIPT_SPAM`/`hasRepeatedSuspectWord`/`isKeywordStuffed`
tonight, so no new phrase was added.

**5. Live checks — could not run, same as last night.** `curl` to
`https://onoutset.com/` and `https://outset-api.onrender.com/health` both
got `403` on the CONNECT tunnel before reaching either host. The proxy's
own status endpoint confirms it: `recentRelayFailures` shows
`connect_rejected` / "gateway answered 403 to CONNECT (policy denial or
upstream failure)" for `onoutset.com:443`. This is the sandbox's own egress
policy, not a live-site problem. CSP/referrer meta tags, API security
headers, the `/health` payload and the CORS answer against
`https://evil.example` were not verified tonight. Needs a run from
somewhere with real internet access, or a manual check.

**6. `.github/workflows`.** Only `.github/workflows/e2e.yml` exists, byte
for byte unchanged since the last sweep (confirmed via `git log` on
`.github/` over the baseline range: no commits). Both `uses:` lines
(`actions/checkout`, `actions/setup-node`, `actions/upload-artifact`) are
pinned to a full commit SHA with a version comment. Top-level
`permissions: contents: read`, no per-job widening, single job never
pushes. Nothing unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. Neither `package.json` nor
`backend/package.json` changed in any of the 33 commits since the baseline.

## Fixed

Nothing needed fixing tonight. Full baseline confirmed green anyway: root
`tsc -b` (clean), root `npm test` (339/339), backend `tsc -p tsconfig.json
--allowImportingTsExtensions` (clean), backend `npm test` (201/201), `npx
vite build` (succeeds, same two JS chunks plus CSS as last night).

## Needs Harshil

- **The 106 already-published listings serving gambling/lottery spam are
  still live** (13 of them also with a spam banner as the cover photo).
  Same set as the 2026-09-17 report named; nothing has changed because the
  fix needs production database access this sandbox doesn't have: re-run
  `isCompromisedText`/`isCompromisedPhrase` against the already-synced
  `facts`/`offerings` rows (not just future crawls), quarantine what trips
  it, and re-run `syncCatalogToApp()`. Full id list is unchanged from last
  night's report.
- **New: a latent CSP gap for the TikTok embed.** `WebListing.tsx`'s
  `TikTokScript` component injects `<script src="https://www.tiktok.com/embed.js">`,
  but `index.html`'s CSP `script-src` only allows `'self' https://js.stripe.com`.
  Not urgent — no listing currently has a `tiktok` field set, so it isn't
  breaking anything live today — but the instant a crawl populates one, a
  real browser enforcing this CSP will block the embed the same way it
  would have blocked Stripe before last night's fix. Worth adding
  `https://www.tiktok.com` to `script-src` (and checking whether the embed
  needs a `frame-src`/`connect-src` entry too) next time someone is in that
  file.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and
  `outset-api.onrender.com`) could not run again — this sandbox's egress
  proxy still blocks both hosts by policy (403 on CONNECT, confirmed via
  the proxy's own status endpoint, not a transient failure). Needs a
  re-run from somewhere with real internet access, or a manual check.
- `public/o/o-easttnscouts-org.json`'s false-positive keyword-stuffing flag
  (a real Scout camp's repeated class schedule) is still there, unchanged;
  still not urgent.
- Everything already on the `docs/SECURITY.md` "only Harshil can do"
  checklist (branch protection, 2FA, Dependabot alerts/secret-scanning
  toggle, Stripe key type, Neon password rotation, GitHub PAT scope,
  `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links) is
  still open; this sweep didn't re-check items that need Render/GitHub
  dashboard access it doesn't have.

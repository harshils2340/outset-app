# Nightly security sweep, 2026-09-23

Baseline is the last sweep, `docs/security/sweeps/2026-09-22.md`, whose head commit was
`124d93dda` (2026-09-21T11:24:52Z). That sweep found nothing new because its own baseline
was already the tip of `main` at the time it ran. During the rest of 2026-09-22, 91 ordinary
commits landed (bug fixes, new booking-system readers, the Viator affiliate feed, an
outreach-email rewrite, the GoDo-to-Outset rename), so tonight's "since the last sweep" window
covers all 91, `124d93dda..1cc46b27`. Head at the start of this sweep: `1cc46b27`
(2026-09-23T02:14:27Z).

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a fresh
`npm ci` in both trees: all four runs came back "found 0 vulnerabilities." Nothing to patch or
bump. `package.json` did not change in either tree across the 91 commits, so there was nothing
new to check either way.

**2. Secret scan.** Ran `git log -p 124d93dda..HEAD` (91 commits, ~744k lines of diff) through
the full pattern set (`postgres(ql)://` with a password, `github_pat_`, `ghp_`, `sk_live_`,
`sk_test_`, `rk_live_`, `whsec_`, `sk-ant-`, `sk-proj-`, Resend/SendGrid/Mailgun key shapes, AWS
key IDs, Google API key shapes, generic `KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to long
random strings), then the same set over the current working tree, including `public/`, `docs/`,
`backend/data/`, fixtures and `.github/`. Findings, all already known from every prior sweep and
none of them Outset's: the `AKIAQHHKRFYJV24PICFC` presigned S3 key ID
(culinarybackstreets.com's own vendor's key, long expired) and the
`AIzaSyCoSDhaFJ0ABL9pv_rTx0iH79EBeXLxPKk` Google-API-key-shaped string (ForeUp's own public
front-end key, documented in a comment in `backend/src/concierge/readers/foreup.ts`). One new
kind of hit tonight, from the new Acuity crawler fixture
`backend/src/enrich/__tests__/fixtures/acuity/exchanges.json`: a captured booking-widget HTML
page carrying that operator's own public client-side tokens (a `RECAPTCHA_SITE_KEY`, a
DataDog `clientToken`, a Square `applicationID`, an `OWNER_KEY`) — the same pattern as the
ForeUp key, a real business's own front-end-only tokens shipped to every browser that loads
their page, not an Outset secret. The several `re_...`-shaped strings the Resend-key pattern
caught were all false positives: substrings inside UUIDs, `claimKey` hashes and crawl-source
URLs, confirmed by reading each one in context. `claimKey` itself, which appears throughout
`public/o/*.json`, is a one-way SHA-256 of an HMAC token (`backend/src/lib/claim.ts`), by design
public-safe; unchanged since baseline and already covered by the 2026-09-16 review. New
`backend/.env.example` added this window has only empty placeholders. No `.env` file, no
`claim-secret.txt`, no `.db` file was added; both `.gitignore`s still exclude them. No
postgres URL with a password anywhere. Nothing live-looking, nothing to rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build`: succeeds, same pre-existing
chunk-size warning as every prior sweep. `index.html`'s CSP is unchanged from the baseline byte
for byte except the GoDo-to-Outset text rename (title, `og:*`, JSON-LD), confirmed with
`git diff` against `124d93dda`: `script-src 'self' https://js.stripe.com`; `style-src`/`font-src`
Google Fonts; `img-src`/`media-src` `'self' http: https:` (deliberately wide, covers hotlinked
operator photos, the wsrv.nl proxy); `connect-src 'self' https://outset-api.onrender.com
https://photon.komoot.io https://api.stripe.com`; `frame-src 'self'` plus YouTube/
YouTube-nocookie/Vimeo/`js.stripe.com`/`hooks.stripe.com`; `object-src 'none'`; `form-action`
Stripe checkout. `Referrer-Policy` meta tag unchanged. Grepped every line added to `src/**/*.ts`
and `src/**/*.tsx` since the baseline for host literals and for the calls that would actually
reach one (`fetch(`, `createElement('script')`, `.src =`, `<iframe`, `<script`): the only new
host strings that turned up (`media.tacdn.com`, `d1a2dkr8rai8e2.cloudfront.net`,
`www.rental.bigmmarina.com`, `share.google`, `www.viator.com`) are either test-fixture data for
the new Viator affiliate feature (an outbound `<a href>` link and a cover image, both already
covered by the wide `img-src` and by ordinary navigation, neither subject to `connect-src` or
`frame-src`) or example URLs inside a doc comment in the new `src/lib/markdown.ts`, whose whole
job is stripping URLs back out of crawled text. No new `fetch`, `<script>` or `<iframe>` target.
`WebListing.tsx`'s `TikTokScript` component is still present and still not in
`script-src`/`frame-src`, same open gap the last several sweeps reported, unchanged tonight.

**4. Hacked-page screen.** Copied `isCompromisedText`/`isCompromisedPhrase` and their helpers
verbatim from `backend/src/sync/contacts.ts` (unchanged since baseline) into a standalone script,
run over every string field, recursively, in `public/catalog.json` and every file in
`public/o/*.json`. Flagged tonight: **12** in `catalog.json`, **20** ids in `public/o/*.json`
(34 raw field hits) — identical counts and identical ids to the 2026-09-22 report. The 14 real
gambling-spam-banner ids and the 6 keyword-stuffing false positives are both exactly the same
lists as last night. No id is new. No spam phrase got past the existing patterns, so no new
phrase was needed tonight.

**5. Live checks, could not run, seventh night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` (plain and with `Origin: https://evil.example`) all got
a `403` on the CONNECT tunnel. The proxy's own status endpoint confirms `recentRelayFailures`:
`connect_rejected`, "gateway answered 403 to CONNECT (policy denial or upstream failure)" for
`onoutset.com:443`, timestamped tonight. Sandbox egress policy, not a live-site problem.
CSP/referrer meta tags, API security headers, the `/health` payload and the CORS answer were not
verified tonight.

**6. `.github/workflows`.** Nothing under `.github/` changed since the baseline (confirmed with
`git diff --stat`). Only `.github/workflows/e2e.yml` exists. All three `uses:` lines
(`actions/checkout@11d5960a...`, `actions/setup-node@49933ea5...`,
`actions/upload-artifact@ea165f8d...`) still pinned to a full commit SHA with a version comment.
Top-level `permissions: contents: read`, no per-job widening, single job never pushes.
`STRIPE_SECRET_KEY`, `RESEND_API_KEY` and `GITHUB_TOKEN` all explicitly empty in the job env; the
only real secret is `E2E_DATABASE_URL`, a scratch Neon branch. Nothing unpinned, nothing
over-scoped, no new workflow.

**7. New dependencies.** None. No commit in the 91-commit window touched `package.json` in
either tree (`git diff --stat 124d93dda..HEAD -- package.json backend/package.json` is empty).

## Fixed

Nothing. Every check came back clean or unchanged from the baseline; nothing clear-cut and small
turned up to fix tonight.

Baseline verified anyway: root `npx tsc -b` clean; backend `npx tsc -p tsconfig.json
--allowImportingTsExtensions` clean; `npx vite build` succeeds; backend `npm test` 672/672 (all
passing — see below); root `npm test` 688/689, one pre-existing failure, not fixed here since
it is application behavior, not a security finding.

Worth noting, not something this sweep did: the second wall-clock-dependent test failure the
2026-09-22 report flagged (`backend/src/concierge/__tests__/live.test.ts:165`) was fixed by
ordinary dev work during the day, unrelated to this sweep — the test now derives its dates from
`Date.now()` instead of a hardcoded date, per its own new comment. Backend tests are fully green
tonight as a result.

## Needs Harshil

- **Root `npm test`'s known failing test, still open**: `src/lib/__tests__/ottoScope.test.ts:214`,
  "a holiday is not answered with today's hours," same wall-clock-dependent assumption every
  prior sweep has flagged. Not fixed tonight for the same reason as always: it is application
  behavior, not a security check.
- **`WebListing.tsx`'s TikTok creator embed, still silently non-functional in production**:
  `https://www.tiktok.com/embed.js` is still blocked by the CSP's `script-src`/`frame-src`, same
  gap the last several sweeps reported. Unchanged tonight; still a product call, not a mechanical
  fix.
- **The published listings still carrying a gambling-spam banner as their cover or gallery
  photo**: the same 14 ids every prior sweep has named (`o-carrefouratlantic-com`,
  `o-dancestudioofmainesouth-com`, `o-dogmasterdistillery-com`, `o-firemensmuseum-com`,
  `o-littlewhiteschoolhouse-com`, `o-mainstreetarmory-com`, `o-markethousemuseum-com`,
  `o-rockysfitnesscenter-com`, `o-rosshillpark-com`, `o-salidagolfclub-com`, `o-suskyriver-com`,
  `o-thejoyofdancingsanjose-com`, `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`). Same
  fix as ever: re-run the hacked-page screen against the already-synced `facts`/`offerings` rows,
  quarantine the cover/photo rows that trip it, and re-sync. This sandbox cannot do it without
  production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-easttnscouts-org`, `o-islandchillyachtcharters-com`, `o-locationdesquatrelacs-com`,
  `o-nortonslc-com`, `o-rfcity-org`, `o-scene75-com`. Not urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`)
  could not run again, seventh sweep in a row. This sandbox's egress proxy still blocks both
  hosts by policy. Needs a re-run from somewhere with real internet access, or a manual check.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch
  protection, 2FA, Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password
  rotation, GitHub PAT scope, `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links)
  is still open; this sweep did not re-check items that need Render/GitHub dashboard access it
  does not have.

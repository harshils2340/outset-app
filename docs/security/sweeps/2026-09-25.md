# Nightly security sweep, 2026-09-25

Baseline is the last sweep, `docs/security/sweeps/2026-09-24.md`, whose head commit was `c3a9bfd0`
(2026-09-23T22:07:13-04:00). During 2026-09-24, 50 ordinary commits landed (a `/where` IP-to-metro
placement feature, the Otto/AI-front-desk page and its outreach campaign, an operator-reviews marquee, a
relevance pass on guest search, `/go` retired in favor of `/where`, and a listing-page fix that moved a
shop's own selling lines out of "Requirements"), so tonight's "since the last sweep" window covers all 50,
`c3a9bfd0..76a6c7a3`. Head at the start of this sweep: `76a6c7a3` (2026-09-24T21:25:34-04:00).

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a fresh `npm ci` in
both trees: all four runs came back "found 0 vulnerabilities." Both lockfiles are byte-for-byte unchanged
since the baseline. `package.json` changed in both trees, but only to add two new npm scripts
(`geo:build` in backend, `search:eval` at root); no dependency was added, removed or bumped in either file.

**2. Secret scan.** Ran `git log -p c3a9bfd0..HEAD` (50 commits, ~710k lines of diff) through the full
pattern set (`postgres(ql)://` with a password, `github_pat_`, `ghp_`, `sk_live_`, `sk_test_`, `rk_live_`,
`whsec_`, `sk-ant-`, `sk-proj-`, AWS key IDs, generic `KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to 20+
character strings), then the same set over the current working tree (`public/`, `docs/`, `backend/data/`,
fixtures, `.github/`). Findings: obvious test fixtures only (`CLAIM_SECRET`/`ADMIN_KEY` literals like
`"a-key-only-the-founder-has"` and `"client-ip-test-secret"` in new test files for the `/where` and
concierge-admin-gate work, `sk_test_recorder`/`whsec_recorder`/`whsec_test` in e2e recorder fixtures, a
placeholder UUID `"aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"`), plus the same already-known, already-expired
`AKIAQHHKRFYJV24PICFC` presigned S3 key ID (culinarybackstreets.com's own vendor's key, cached in
`public/o/o-culinarybackstreets-com.json` and `backend/data/photos/shard-1-of-10.json`) every prior sweep has
reported. No new kind of hit tonight. No `.env` file, no `claim-secret.txt`, no `.db` file was added. No
postgres URL with a password anywhere. Nothing live-looking, nothing to rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build`: succeeds, same pre-existing chunk-size warning
as every prior sweep. `index.html` is byte-for-byte unchanged from the baseline: CSP still `script-src 'self'
https://js.stripe.com`; `style-src`/`font-src` Google Fonts; `img-src`/`media-src` `'self' http: https:`;
`connect-src 'self' https://outset-api.onrender.com https://photon.komoot.io https://api.stripe.com`;
`frame-src 'self'` plus YouTube/YouTube-nocookie/Vimeo/`js.stripe.com`/`hooks.stripe.com`; `object-src
'none'`; `form-action` Stripe checkout. `Referrer-Policy` meta tag unchanged. Grepped every line added to
`src/**/*.ts` and `src/**/*.tsx` since the baseline for host literals and for the calls that would actually
reach one (`fetch(`, `createElement('script')`, `.src =`, `<iframe`, `<script`): none of the new-line
patterns matched. The three new host literals that turned up
(`db-ip.com`, `keen-fly.com`, `viator.com`) are, respectively, a required CC BY 4.0 attribution `<a href>`
link for the new `/where` IP-geolocation feature, plain text inside a scraped operator's own cancellation
policy string, and Viator affiliate `<a href>` link data, exactly the same shape the last sweep already
covers. No new `fetch`, `<script>` or `<iframe>` target appears anywhere in the diff.
`WebListing.tsx`'s `TikTokScript` component is still present and still not in `script-src`/`frame-src`, same
open gap the last several sweeps reported, unchanged tonight.

**4. Hacked-page screen.** Copied `isCompromisedPhrase`/`isCompromisedText` and their helpers verbatim from
`backend/src/sync/contacts.ts` (unchanged since baseline, `git diff` empty) into a standalone script, run over
every string field, recursively, in `public/catalog.json` and every file in `public/o/*.json`. Flagged
tonight: **11** in `catalog.json`, **19** ids in `public/o/*.json`, identical counts and identical id lists to
the prior sweep, no id added or removed. The 13 real gambling-spam-banner ids and the same 6
keyword-stuffing false positives are unchanged. No id is new. No spam phrase got past the existing patterns,
so no new phrase was needed tonight.

**5. Live checks, could not run, ninth night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` both got a `403` from the sandbox's own egress gateway on the
CONNECT tunnel (confirmed against the proxy's own `/__agentproxy/status`: `recentRelayFailures` shows
`connect_rejected`, "gateway answered 403 to CONNECT (policy denial or upstream failure)" for both hosts,
timestamped tonight). Sandbox egress policy, not a live-site problem. CSP/referrer meta tags, API security
headers, the `/health` payload and the CORS answer against `Origin: https://evil.example` were not verified
tonight.

**6. `.github/workflows`.** Nothing under `.github/` changed since the baseline (`git diff --stat` empty).
Only `.github/workflows/e2e.yml` exists. All three `uses:` lines (`actions/checkout@11d5960a...`,
`actions/setup-node@49933ea5...`, `actions/upload-artifact@ea165f8d...`) still pinned to a full commit SHA
with a version comment. Top-level `permissions: contents: read`, no per-job widening, single job never
pushes. Nothing unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. No commit in the 50-commit window added, removed or bumped a dependency in
either tree's `package.json` or lockfile (see check 1: the only `package.json` changes were two new npm
scripts).

## Baseline verified

Root `npx tsc -b` clean. Backend `npx tsc -p tsconfig.json --allowImportingTsExtensions` clean. `npx vite
build` succeeds. Backend `npm test`: 762 tests, 760 pass, 0 fail, 2 skipped. Root `npm test`: 805 tests, 805
pass, 0 fail, 0 skipped.

Both root test failures the last sweep reported under Needs Harshil are gone tonight, fixed by ordinary dev
work, not by this sweep: commit `7e95fcf1` ("A shop's own selling lines stop being headed 'Requirements' on
its /l/ page") changed how the static listing page sorts a shop's `specs` lines between "Who can go" and a
new Highlights section, which both made `cardNotice.test.ts`'s stale assertion correct again and fixed the
Viator listing (`a-viator-100569p1`) printing one policy line 5 times instead of 3 that `policyLines.test.ts`
was catching.

## Fixed

Nothing. Every check came back clean or unchanged from the baseline; nothing clear-cut and small in the
sweep's own categories (a dependency bump, a new spam phrase, a re-pinned action, a removed stray secret)
turned up to fix tonight. No commit was made.

## Needs Harshil

- **The published listings still carrying a gambling-spam banner as their cover or gallery photo**: 13 ids,
  unchanged from the last sweep: `o-carrefouratlantic-com`, `o-dancestudioofmainesouth-com`,
  `o-dogmasterdistillery-com`, `o-firemensmuseum-com`, `o-littlewhiteschoolhouse-com`,
  `o-mainstreetarmory-com`, `o-markethousemuseum-com`, `o-rockysfitnesscenter-com`, `o-rosshillpark-com`,
  `o-salidagolfclub-com`, `o-suskyriver-com`, `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`. Same fix
  as ever: re-run the hacked-page screen against the already-synced `facts`/`offerings` rows, quarantine the
  cover/photo rows that trip it, and re-sync. This sandbox cannot do it without production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-easttnscouts-org`, `o-islandchillyachtcharters-com`, `o-locationdesquatrelacs-com`, `o-nortonslc-com`,
  `o-rfcity-org`, `o-scene75-com`. Not urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`) could not run
  again, ninth sweep in a row. This sandbox's egress proxy still blocks both hosts by policy. Needs a re-run
  from somewhere with real internet access, or a manual check.
- `WebListing.tsx`'s `TikTokScript` component is still not covered by `script-src`/`frame-src` in the CSP,
  same open gap the last several sweeps reported. Not urgent (nothing currently reaches it that isn't already
  same-origin), but worth a deliberate decision: either add the TikTok embed host to the CSP or confirm the
  component is unused and remove it.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch protection, 2FA,
  Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password rotation, GitHub PAT scope,
  `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links) is still open; this sweep did not
  re-check items that need Render/GitHub dashboard access it does not have.

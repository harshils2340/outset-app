# Nightly security sweep, 2026-09-22

Baseline is the last sweep, `docs/security/sweeps/2026-09-21.md`, whose head commit was
`124d93dda` (2026-09-21T11:24:52Z). Only one commit landed since then, `124d93dda` itself being
the baseline's own head — checking `git log --since` against that timestamp turns up no further
commits, so "since the last sweep" below covers a clean, empty window on the code side.

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a fresh
`npm ci` in both trees: all four runs came back "found 0 vulnerabilities." Nothing to patch or
bump.

**2. Secret scan.** No commits landed since the baseline, so there was nothing new to scan there.
Scanned the full working tree anyway, including `public/`, `docs/`, `backend/data/`, fixtures and
`.github/`, for `postgres(ql)://` URLs with a password, `github_pat_`, `ghp_`, `sk_live_`,
`sk_test_`, `rk_live_`, `whsec_`, `sk-ant-`, `sk-proj-`, AWS key IDs, Google API key shapes, and
generic `KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to long random strings. Same known,
unchanged hits as every prior sweep: the `AKIAQHHKRFYJV24PICFC` presigned S3 key ID in
`public/o/o-culinarybackstreets-com.json` and `backend/data/photos/shard-1-of-10.json`
(culinarybackstreets.com's own vendor's key, long expired, not Outset's), and the
`AIzaSyCoSDhaFJ0ABL9pv_rTx0iH79EBeXLxPKk` Google-API-key-shaped string in a doc comment in
`backend/src/concierge/readers/foreup.ts`, documented there as ForeUp's own public front-end key
shipped to every browser that loads any course's booking widget. Everything else the patterns
caught (`sk_test_recorder`, `whsec_test_verifyWebhook`, test `CLAIM_SECRET`/`ADMIN_KEY` fixtures,
a placeholder `KEY` constant in a concierge test) is a test constant, same as every prior sweep.
No `.env` file in the repo; both `.gitignore`s still exclude it. No postgres URL with a password
anywhere. Nothing live-looking, nothing to rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build`: succeeds, same pre-existing
chunk-size warning as every prior sweep. `index.html`'s CSP is byte-for-byte unchanged from the
baseline: `script-src 'self' https://js.stripe.com`; `style-src`/`font-src` Google Fonts;
`img-src`/`media-src` `'self' http: https:` (deliberately wide, covers hotlinked operator photos
including the wsrv.nl proxy); `connect-src 'self' https://outset-api.onrender.com
https://photon.komoot.io https://api.stripe.com`; `frame-src 'self'` plus YouTube/Vimeo/Stripe;
`object-src 'none'`; `form-action` Stripe checkout. `Referrer-Policy` meta tag
(`strict-origin-when-cross-origin`) also unchanged. Grepped every `.ts`/`.tsx` file under `src/`
for host literals and for the DOM/network calls that would actually reach one
(`fetch(`, `createElement('script')`, `.src =`, `<iframe`): every `fetch` goes to the API, the
Photon geocoder, or the app's own static JSON; the only externally-loaded script is
`stripeJs.ts`'s `js.stripe.com`, already allowed. `WebListing.tsx`'s `TikTokScript` component
(`https://www.tiktok.com/embed.js`) is still present and still not in `script-src`/`frame-src` —
the same gap the 2026-09-21 sweep reported, unchanged tonight, still open as a product decision
rather than a same-night CSP change. No new host anywhere.

**4. Hacked-page screen.** Copied `isCompromisedText`/`isCompromisedPhrase` and their helpers
verbatim from `backend/src/sync/contacts.ts` (unchanged since the baseline) into a standalone
script under `/tmp`, run over every string field, recursively, in `public/catalog.json` and
every file in `public/o/*.json`. Flagged tonight: **12** in `catalog.json`, **20** ids in
`public/o/*.json` (34 raw field hits) — identical counts and identical ids to the 2026-09-21
report, including `o-windsorgymnastics-org`'s unchanged `blogger.googleusercontent.com` cover.
The 6 `isKeywordStuffed` hits (`o-easttnscouts-org`, `o-islandchillyachtcharters-com`,
`o-locationdesquatrelacs-com`, `o-nortonslc-com`, `o-rfcity-org`, `o-scene75-com`) are the same
real-rate-card/scraping-artifact false positives every prior sweep named. No id is new. No spam
phrase got past the existing patterns, so no new phrase was needed tonight.

**5. Live checks, could not run, sixth night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` both got a `403` on the CONNECT tunnel. The proxy's own
status endpoint confirms `recentRelayFailures`: `connect_rejected`, "gateway answered 403 to
CONNECT (policy denial or upstream failure)" for both hosts, timestamped tonight. Sandbox egress
policy, not a live-site problem. CSP/referrer meta tags, API security headers, the `/health`
payload and the CORS answer against `https://evil.example` were not verified tonight.

**6. `.github/workflows`.** Only `.github/workflows/e2e.yml` exists; nothing under `.github/`
changed since the baseline. All three `uses:` lines (`actions/checkout@11d5960a...`,
`actions/setup-node@49933ea5...`, `actions/upload-artifact@ea165f8d...`) still pinned to a full
commit SHA with a version comment. Top-level `permissions: contents: read`, no per-job widening,
single job never pushes. `STRIPE_SECRET_KEY`, `RESEND_API_KEY` and `GITHUB_TOKEN` all explicitly
empty in the job env; the only real secret is `E2E_DATABASE_URL`, a scratch Neon branch. Nothing
unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. No commit since the baseline touched `package.json` or either
`package-lock.json`.

## Fixed

Nothing. Every check came back clean or unchanged from the baseline. No `npm audit` bump, no
stray secret, no new spam phrase, no workflow to re-pin, no new host to allow or block.

Baseline verified anyway: root `npx tsc -b` clean; backend `npx tsc -p tsconfig.json
--allowImportingTsExtensions` clean; `npx vite build` succeeds; backend `npm test` 615/616; root
`npm test` 621/622. Both single failures are pre-existing, non-security, wall-clock-dependent
test design issues — see "Needs Harshil" below — not fixed here since they are application
behavior, not security findings, and outside tonight's fix scope.

## Needs Harshil

- **A second wall-clock-dependent test failure, found tonight**:
  `backend/src/concierge/__tests__/live.test.ts:165`, "the window is the shop's own calendar day,
  not the host's," expects `["Sunset flight"]` and got `[]`. The test hardcodes its dates
  (`evening = new Date("2026-09-22T01:00:00Z")`), but the departure-filtering helper it exercises,
  `departed()` in `backend/src/concierge/live.ts:195`, defaults its `now` argument to the real
  `Date.now()` rather than the test's injected reference time, and the call site at
  `live.ts:278` never passes one. Once real wall-clock time passed the test's fixed "Sunset
  flight" departure (2026-09-21T22:30:00-04:00, i.e. 2026-09-22T02:30:00Z), the test started
  failing against the actual server clock instead of the scenario it was written to check. Same
  shape as the `ottoScope.test.ts` failure the last five sweeps have carried forward: a real test
  design gap (a live-network reader's date-filtering logic silently anchored to real time inside
  a test meant to be time-independent), not a security issue, and not fixed tonight since it's
  outside this sweep's remit. Worth a look because the same anchoring-to-real-time pattern, if it
  ever reached the actual booking-availability path rather than just this test, is the kind of
  bug that quietly drops still-bookable slots near a shop's closing time.
- **Root `npm test`'s known failing test, still open**: `src/lib/__tests__/ottoScope.test.ts:231`,
  "a holiday is not answered with today's hours," same wall-clock-dependent assumption every
  prior sweep has flagged. Not fixed tonight for the same reason as always: it's application
  behavior, not a security check.
- **`WebListing.tsx`'s TikTok creator embed, still silently non-functional in production**:
  `https://www.tiktok.com/embed.js` is still blocked by the CSP's `script-src`/`frame-src`, same
  gap the 2026-09-21 sweep reported. Unchanged tonight; still a product call, not a mechanical
  fix.
- **The published listings still carrying a gambling-spam banner as their cover or gallery
  photo**: the same 14 ids every prior sweep has named (`o-carrefouratlantic-com`,
  `o-dancestudioofmainesouth-com`, `o-dogmasterdistillery-com`, `o-firemensmuseum-com`,
  `o-littlewhiteschoolhouse-com`, `o-mainstreetarmory-com`, `o-markethousemuseum-com`,
  `o-rockysfitnesscenter-com`, `o-rosshillpark-com`, `o-salidagolfclub-com`, `o-suskyriver-com`,
  `o-thejoyofdancingsanjose-com`, `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`). Same
  fix as ever: re-run the hacked-page screen against the already-synced `facts`/`offerings` rows,
  quarantine the cover/photo rows that trip it, and re-sync. This sandbox cannot do it without
  production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-locationdesquatrelacs-com`, `o-scene75-com`, `o-rfcity-org`, `o-nortonslc-com`. Not urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`)
  could not run again, sixth sweep in a row. This sandbox's egress proxy still blocks both hosts
  by policy. Needs a re-run from somewhere with real internet access, or a manual check.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch
  protection, 2FA, Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password
  rotation, GitHub PAT scope, `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links)
  is still open; this sweep didn't re-check items that need Render/GitHub dashboard access it
  doesn't have.

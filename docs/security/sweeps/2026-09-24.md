# Nightly security sweep, 2026-09-24

Baseline is the last sweep, `docs/security/sweeps/2026-09-23.md`, whose head commit was `1cc46b27`
(2026-09-23T02:14:27Z). During the rest of 2026-09-23, 80 ordinary commits landed (the Viator affiliate
launch and detail pass, two new discovery directories, an unclaimed-listing charge bug fix, five bug bash
runs, an hours-parsing rewrite), so tonight's "since the last sweep" window covers all 80,
`1cc46b27..c3a9bfd0`. Head at the start of this sweep: `c3a9bfd0` (2026-09-23T22:07:13-04:00). The checkout
arrived shallow (50 commits); it was unshallowed to reach the baseline commit before diffing.

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a fresh `npm ci` in
both trees: all four runs came back "found 0 vulnerabilities." `package.json` and both lockfiles are
byte-for-byte unchanged since the baseline across all 80 commits (`git diff --stat` empty), so there was
nothing new to check either way.

**2. Secret scan.** Ran `git log -p 1cc46b27..HEAD` (80 commits, ~421k lines of diff) through the full
pattern set (`postgres(ql)://` with a password, `github_pat_`, `ghp_`, `sk_live_`, `sk_test_`, `rk_live_`,
`whsec_`, `sk-ant-`, `sk-proj-`, AWS key IDs, Google API key shapes, generic
`KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to 20+ character strings), then the same set over the current
working tree (`public/`, `docs/`, `backend/data/`, fixtures, `.github/`). Findings, all already known from
every prior sweep and none of them Outset's: the `AKIAQHHKRFYJV24PICFC` presigned S3 key ID
(culinarybackstreets.com's own vendor's key, long expired, in `public/o/o-culinarybackstreets-com.json` and
`docs/security/2026-09-16-keys-api.md`) and the same key ID inside `backend/data/photos/shard-1-of-10.json`
(the same expired presigned URL, cached from the same crawl). No new kind of hit tonight. No `.env` file, no
`claim-secret.txt`, no `.db` file was added; `backend/.env.example` (tracked, added before the baseline) still
carries only empty placeholders and commented-out sample addresses. Both `.gitignore`s still exclude real env
files and local databases. No postgres URL with a password anywhere. Nothing live-looking, nothing to
rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build`: succeeds, same pre-existing chunk-size warning
as every prior sweep. `index.html` is byte-for-byte unchanged from the baseline (`git diff` empty):
`script-src 'self' https://js.stripe.com`; `style-src`/`font-src` Google Fonts; `img-src`/`media-src` `'self'
http: https:`; `connect-src 'self' https://outset-api.onrender.com https://photon.komoot.io
https://api.stripe.com`; `frame-src 'self'` plus YouTube/YouTube-nocookie/Vimeo/`js.stripe.com`/
`hooks.stripe.com`; `object-src 'none'`; `form-action` Stripe checkout. `Referrer-Policy` meta tag unchanged.
Grepped every line added to `src/**/*.ts` and `src/**/*.tsx` since the baseline for host literals and for the
calls that would actually reach one (`fetch(`, `createElement('script')`, `.src =`, `<iframe`, `<script`):
the new host strings that turned up (`media.tacdn.com`, test fixtures under `viator.example`, cruise and
watersports operator domains) are all Viator affiliate `<a href>` link data or test fixture content, already
covered by the wide `img-src` and by ordinary navigation. No new `fetch`, `<script>` or `<iframe>` target
appears anywhere in the diff. `WebListing.tsx`'s `TikTokScript` component is still present and still not in
`script-src`/`frame-src`, same open gap the last several sweeps reported, unchanged tonight.

**4. Hacked-page screen.** Copied `isCompromisedPhrase`/`isCompromisedText` and their helpers verbatim from
`backend/src/sync/contacts.ts` (unchanged since baseline) into a standalone script, run over every string
field, recursively, in `public/catalog.json` and every file in `public/o/*.json`. Flagged tonight: **11** in
`catalog.json` (down from 12; `catalog.json` only carries a `cover` field, not the full photo gallery, so it
always flags fewer entries than `public/o/*.json`), **19** ids in `public/o/*.json` (down from 20). The drop
by one is real and good: `o-thejoyofdancingsanjose-com`'s gambling-spam cover banner was removed (`"cover"`
dropped, `"photos":[]`) in the Viator sync commit `58fafa17` on 2026-09-23, ordinary dev work, not this
sweep. The remaining **13** real gambling-spam-banner ids and the same **6** keyword-stuffing false positives
are identical to every prior sweep. No id is new. No spam phrase got past the existing patterns, so no new
phrase was needed tonight.

**5. Live checks, could not run, eighth night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` (plain and with `Origin: https://evil.example`) all got `HTTP_STATUS:000`
(connection failed). The proxy's own status endpoint confirms `recentRelayFailures`: `connect_rejected`,
"gateway answered 403 to CONNECT (policy denial or upstream failure)" for both `onoutset.com:443` and
`outset-api.onrender.com:443`, timestamped tonight. Sandbox egress policy, not a live-site problem.
CSP/referrer meta tags, API security headers, the `/health` payload and the CORS answer were not verified
tonight.

**6. `.github/workflows`.** Nothing under `.github/` changed since the baseline (`git diff --stat` empty).
Only `.github/workflows/e2e.yml` exists. All three `uses:` lines (`actions/checkout@11d5960a...`,
`actions/setup-node@49933ea5...`, `actions/upload-artifact@ea165f8d...`) still pinned to a full commit SHA
with a version comment. Top-level `permissions: contents: read`, no per-job widening, single job never
pushes. Nothing unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. No commit in the 80-commit window touched `package.json` or either lockfile in
either tree.

## Baseline verified

Root `npx tsc -b` clean. Backend `npx tsc -p tsconfig.json --allowImportingTsExtensions` clean. `npx vite
build` succeeds. Backend `npm test`: 739 tests, 737 pass, 0 fail, 2 skipped. Root `npm test`: 742 tests, 740
pass, 2 fail, neither of them touched here (see Needs Harshil): both are pre-existing application-behavior
gaps, not security findings, and neither fix is clear-cut and small in the sense this sweep is scoped to.

## Fixed

Nothing. Every check came back clean or unchanged from the baseline (or, in the case of the hacked-page
screen, improved by ordinary dev work); nothing clear-cut and small in the sweep's own categories (a
dependency bump, a new spam phrase, a re-pinned action, a removed stray secret) turned up to fix tonight.

## Needs Harshil

- **`src/lib/__tests__/cardNotice.test.ts:43` now fails, but the underlying change is a real fix, not a
  regression.** Commit `c7dd2fc3`'s neighbourhood (2026-09-23, "A partner's product is not ours to claim")
  and a same-day fix to `backend/src/api/bookings.ts` changed `payNow` from
  `stripeEnabled() && !!priced && priced.total >= 1` to `!!profile && stripeEnabled() && !!priced &&
  priced.total >= 1`. The added `!!profile` guard closes a real gap the code comment describes: an unclaimed
  listing could hold a guest's card for a trip an operator never claimed, agreed to sell through Outset, or
  even knows exists there, found 23 September from a real operator's complaint. The fix is already live in
  this branch; the test's exact-match regex is just stale against it. This sweep did not touch the test,
  since updating a test assertion is outside the enumerated categories this sweep fixes, but it is a one-line
  regex update, not a design question, and would be a safe pickup for ordinary dev work.
- **`src/lib/__tests__/policyLines.test.ts:62` fails**, a data-consistency check over the whole catalog: a
  Viator affiliate listing (`a-viator-100569p1`) prints one policy line 5 times instead of 3 (some line
  landing in two columns). Content/data correctness in the new Viator detail pass, not a security finding;
  not fixed here for the same reason the wall-clock test failures never have been.
- **The published listings still carrying a gambling-spam banner as their cover or gallery photo**: 13 ids
  now, down from 14 (`o-thejoyofdancingsanjose-com` was fixed by ordinary dev work overnight, see check 4).
  The 13 still open: `o-carrefouratlantic-com`, `o-dancestudioofmainesouth-com`, `o-dogmasterdistillery-com`,
  `o-firemensmuseum-com`, `o-littlewhiteschoolhouse-com`, `o-mainstreetarmory-com`, `o-markethousemuseum-com`,
  `o-rockysfitnesscenter-com`, `o-rosshillpark-com`, `o-salidagolfclub-com`, `o-suskyriver-com`,
  `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`. Same fix as ever: re-run the hacked-page screen
  against the already-synced `facts`/`offerings` rows, quarantine the cover/photo rows that trip it, and
  re-sync. This sandbox cannot do it without production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-easttnscouts-org`, `o-islandchillyachtcharters-com`, `o-locationdesquatrelacs-com`, `o-nortonslc-com`,
  `o-rfcity-org`, `o-scene75-com`. Not urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`) could not run
  again, eighth sweep in a row. This sandbox's egress proxy still blocks both hosts by policy. Needs a re-run
  from somewhere with real internet access, or a manual check.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch protection, 2FA,
  Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password rotation, GitHub PAT scope,
  `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links) is still open; this sweep did not
  re-check items that need Render/GitHub dashboard access it does not have.

# Nightly security sweep, 2026-09-26

Baseline is the last sweep, `docs/security/sweeps/2026-09-25.md`, whose head commit was `76a6c7a3`
(2026-09-24T21:25:34-04:00). During 2026-09-25, 33 ordinary commits landed (the ARIA fix for the service
picker and tab strips, the "quiet API" fix for claim rehearsal, the kids-filter rule reaching the browse
catalog, several "More like this" and listing-link dedup fixes, the em-dash tidy learning a day and a clock
time, the Otto page rebuild, and Otto's new grounded fallback through Cohere), so tonight's "since the last
sweep" window covers all 33, `76a6c7a3..c7aec11e`. Head at the start of this sweep: `c7aec11e`
(2026-09-25T19:42:04-04:00).

## Checked

**1. `npm audit`.** Root tree and `backend/`, each with and without `--omit=dev`, after a fresh `npm ci` in
both trees: all four runs came back "found 0 vulnerabilities." Both lockfiles are byte-for-byte unchanged
since the baseline. Root `package.json` changed only to add one new npm script (`otto:eval`); backend's
`package.json` did not change at all. No dependency was added, removed or bumped in either tree.

**2. Secret scan.** Ran `git log -p 76a6c7a3..HEAD` (33 commits) through the full pattern set
(`postgres(ql)://` with a password, `github_pat_`, `ghp_`, `sk_live_`, `sk_test_`, `rk_live_`, `whsec_`,
`sk-ant-`, `sk-proj-`, AWS key IDs, generic `KEY`/`SECRET`/`TOKEN`/`PASSWORD` assignments to 20+ character
strings), then the same set over the current working tree (`public/`, `docs/`, `backend/data/`, fixtures,
`.github/`). The window added Otto's new Cohere-backed grounded fallback (`backend/src/lib/cohere.ts`,
`backend/src/api/otto.ts`, `backend/.env.example`); checked its diff by hand: the key is read from
`process.env.COHERE_API_KEY` at call time and `backend/.env.example` only adds the placeholder line
(`COHERE_API_KEY=`) plus a comment, no real key. Findings otherwise: the same already-known hits every prior
sweep has reported, nothing new. `git log -p` only turned up `sk_test_recorder`/`whsec_recorder`/`whsec_test`
inside the *text of the previous sweep's own report* (added in this window as `docs/security/sweeps/2026-09-25.md`,
which quotes those e2e recorder fixture names), and the same reference to the already-known, already-expired
`AKIAQHHKRFYJV24PICFC` presigned S3 key. The working-tree scan hit the same three files every prior sweep has
flagged: `public/o/o-culinarybackstreets-com.json` and `backend/data/photos/shard-1-of-10.json` (the expired
S3 key, culinarybackstreets.com's own vendor's key), plus the sweep-report `.md` files under `docs/` quoting
past findings. No `.env` file, no `claim-secret.txt`, no `.db` file was added. No postgres URL with a password
anywhere. Nothing live-looking, nothing to rotate.

**3. Third-party host drift.** Ran a fresh `npx vite build`: succeeds, same pre-existing chunk-size warning
as every prior sweep. `index.html` is byte-for-byte unchanged from the baseline: same CSP, same
`Referrer-Policy` meta tag. Grepped every line added to `src/**/*.ts` and `src/**/*.tsx` since the baseline
for host literals and for the calls that would actually reach one (`fetch(`, `createElement('script')`,
`.src =`, `<iframe`, `<script`). The only reach call is a new `fetch(API_URL + "/otto/ask")`, same-origin to
the already-allowlisted API host. The new host literals in the diff (`example.com`, `outset.app`,
`viator.example`, `www.google.com`) are all in test fixtures or `<a href>` Maps/Viator links, not a script,
style, connect or frame target, the same shape prior sweeps already cover. `WebListing.tsx`'s `TikTokScript`
component is still present and still not in `script-src`/`frame-src`, same open gap the last several sweeps
reported, unchanged tonight.

**4. Hacked-page screen.** Copied `isCompromisedPhrase`/`isCompromisedText` and their helpers verbatim from
`backend/src/sync/contacts.ts` (unchanged since baseline for these functions; the file did change this
window, but only for `tidyDashes`, `compactDeal` and a new `kid` field, none of which touch the spam screen)
into a standalone script, run over every string field, recursively, in `public/catalog.json` and every file
in `public/o/*.json`. Flagged tonight: **11** in `catalog.json`, **19** ids in `public/o/*.json`, identical
counts and identical id lists to the prior sweep, no id added or removed. The same 13 real gambling-spam-banner
ids and the same 6 keyword-stuffing false positives are unchanged. No id is new. No spam phrase got past the
existing patterns, so no new phrase was needed tonight.

**5. Live checks, could not run, tenth night in a row.** `curl` to `https://onoutset.com/` and
`https://outset-api.onrender.com/health` both got a `403` from the sandbox's own egress gateway on the
CONNECT tunnel (confirmed against the proxy's own `/__agentproxy/status`: `recentRelayFailures` shows
`connect_rejected`, "gateway answered 403 to CONNECT (policy denial or upstream failure)" for both hosts,
timestamped 2026-09-26T03:37:58Z and 03:38:09Z). Sandbox egress policy, not a live-site problem.
CSP/referrer meta tags, API security headers, the `/health` payload and the CORS answer against
`Origin: https://evil.example` were not verified tonight.

**6. `.github/workflows`.** Nothing under `.github/` changed since the baseline (`git diff --stat` empty).
Only `.github/workflows/e2e.yml` exists. All three `uses:` lines (`actions/checkout@11d5960a...`,
`actions/setup-node@49933ea5...`, `actions/upload-artifact@ea165f8d...`) still pinned to a full commit SHA
with a version comment. Top-level `permissions: contents: read`, no per-job widening, single job never
pushes. Nothing unpinned, nothing over-scoped, no new workflow.

**7. New dependencies.** None. No commit in the 33-commit window added, removed or bumped a dependency in
either tree's `package.json` or lockfile (see check 1: the only `package.json` change anywhere was root's new
`otto:eval` script). The new Cohere integration calls the API with plain `fetch`, no new SDK package.

## Baseline verified

Root `npx tsc -b` clean. Backend `npx tsc -p tsconfig.json --allowImportingTsExtensions` clean. `npx vite
build` succeeds. Root `npm test`: 871 tests, 871 pass, 0 fail, 0 skipped. Backend `npm test`: 810 tests, 808
pass, 0 fail, 2 skipped.

## Fixed

Nothing. Every check came back clean or unchanged from the baseline; nothing clear-cut and small in the
sweep's own categories (a dependency bump, a new spam phrase, a re-pinned action, a removed stray secret)
turned up to fix tonight. No commit was made.

## Needs Harshil

- **The published listings still carrying a gambling-spam banner as their cover or gallery photo**: 13 ids,
  unchanged from the last sweep: `o-carrefouratlantic-com`, `o-dancestudioofmainesouth-com`,
  `o-dogmasterdistillery-com`, `o-firemensmuseum-com`, `o-littlewhiteschoolhouse-com`,
  `o-mainstreetarmory-com`, `o-markethousemuseum-com`, `o-rockysfitnesscenter-com`, `o-rosshillpark-com`,
  `o-salidagolfclub-com`, `o-suskyriver-com`, `o-timberviewgolfclub-com`, `o-windsorgymnastics-org`. Same fix
  as ever: re-run the hacked-page screen against the already-synced `facts`/`offerings` rows, quarantine the
  cover/photo rows that trip it, and re-sync. This sandbox cannot do it without production database access.
- The recurring likely-false-positive keyword-stuffing flags, still open, still harmless:
  `o-easttnscouts-org`, `o-islandchillyachtcharters-com`, `o-locationdesquatrelacs-com`, `o-nortonslc-com`,
  `o-rfcity-org`, `o-scene75-com`. Not urgent.
- Check 5 (live CSP/header/CORS checks against `onoutset.com` and `outset-api.onrender.com`) could not run
  again, tenth sweep in a row. This sandbox's egress proxy still blocks both hosts by policy. Needs a re-run
  from somewhere with real internet access, or a manual check.
- `WebListing.tsx`'s `TikTokScript` component is still not covered by `script-src`/`frame-src` in the CSP,
  same open gap the last several sweeps reported. Not urgent (nothing currently reaches it that isn't already
  same-origin), but worth a deliberate decision: either add the TikTok embed host to the CSP or confirm the
  component is unused and remove it.
- New this window: Otto's grounded fallback now calls out to Cohere (`backend/src/lib/cohere.ts`) whenever the
  `COHERE_API_KEY` env var is set on a host. Worth confirming on the Harshil-only checklist that any Cohere
  key placed on Render is a trial or scoped key, not shared with any other service, and that
  `COHERE_DAILY_CAP`/`COHERE_RPM` are set to something Harshil is comfortable paying for if the trial key is
  ever swapped for a paid one.
- Everything already on the `docs/SECURITY.md` "only Harshil can do" checklist (branch protection, 2FA,
  Dependabot alerts/secret-scanning toggle, Stripe key type, Neon password rotation, GitHub PAT scope,
  `OUTSET_TEST_CLAIM_EMAILS` on Render, old non-expiring claim links) is still open; this sweep did not
  re-check items that need Render/GitHub dashboard access it does not have.
